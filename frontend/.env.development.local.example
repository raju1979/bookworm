# Optional personal overrides for LOCAL ONLY (gitignored via *.local)
# Do NOT put production URLs here — Vite also loads .env.local during `vite build`
# and it would override .env.production.
#
# Example: use live API while developing UI locally:
# VITE_API_URL=https://ajarafashion.com/bookworm/backend
