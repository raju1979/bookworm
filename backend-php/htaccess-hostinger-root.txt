RewriteEngine On

# Block direct access to secrets / source
RewriteRule ^(app|lib|tmp)/ - [F,L]
RewriteRule ^\.env - [F,L]

# Send all traffic into public/ (so URL is /bookworm/... not /bookworm/public/...)
RewriteRule ^$ public/ [L]
RewriteRule ^(.*)$ public/$1 [L]
